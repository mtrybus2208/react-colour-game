//import * as actionTypes from './../actions/actionTypes';

const initialState = {
  newQuotes: {
    value: '',
    error: null
  } 
};

export const newGameReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
    }
};